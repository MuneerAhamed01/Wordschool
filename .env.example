# Copy to `.env` for legacy single-env runs, or use flavor-specific files:
#   `.env.prod` — production flavor
#   `.env.dev`  — development flavor (default)
#
# Monetization & in-app purchases (AdMob, IAP). Keep false until the app is stable.
IS_MONIT_PURCH=false
