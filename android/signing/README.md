# Android release signing

Place your release signing assets in this folder:

- `upload-keystore.jks` — upload keystore (never commit)
- `upload_certificate.pem` — Play App Signing upload certificate (never commit)

Copy `../key.properties.example` to `../key.properties` and fill in your values.
